language: go

go:
  - "1.15"

env:
  global:
    - GO111MODULE=on
    - PGPORT=5432
    - PGUSER=postgres
    - PGPASSWORD=
    - PGDATABASE=postgres

services:
  - mysql
  - postgresql

before_install:
  - mysql -e 'CREATE DATABASE test;'

install:
  - go mod download

script:
  - ./build+test.sh
